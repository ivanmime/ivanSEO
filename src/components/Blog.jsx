import React from 'react';

/**
 * Blog component removed.
 * Kept as a stub (renders nothing) to avoid import errors.
 * If you prefer to delete this file entirely, remove it from the repo.
 */
const Blog = () => null;

export default Blog;
